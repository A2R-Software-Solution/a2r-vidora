# Production release evidence — 2026-09-09

Release status: **deployed, not approved for unrestricted production use**.

## Completed and observed

- Private PostgreSQL backup saved in the git-ignored and deployment-excluded
  `functions/secrets/release-backups/` directory before migration. This contains
  private application data; restrict access and apply the owner's backup policy.
  A restore drill has not been performed.
- Database migration `6c10a9e721df` applied and re-read successfully.
- Atomic shared budget live check: 12 independent connections, exactly 5 admitted
  and 7 rejected. Only its generated test key was removed afterward. This does not
  prove concurrent video-submission idempotency or edge abuse resistance.
- Firebase Functions deployment completed. API and processvideo verified ACTIVE;
  API updated 2026-09-09T11:48:12Z, production environment, concurrency 2, maximum
  10 instances. No minimum-instance reservation added.
- 13 offline backend tests passed; model SHA-256 verified; frontend lint/build
  passed; production npm audit reported zero vulnerabilities at test time.
  Python dependency vulnerability scanning/locking remains outstanding.
- Live warm-up returned 200, health returned 200, anonymous video listing denied
  with 401. First one-minute analysis returned 200 after 71.99 seconds. Same-key
  sequential replay returned the saved result.
- Second one-minute analysis returned 502 after 4.02 seconds. Its download stage
  failed after 1.42 seconds; YouTube required bot/session confirmation for
  `youtube-cookies-account-2`. The first successful run selected account-1.
  This is a real reliability blocker, not evidence that cached inference is slow.
- Five live synthetic Groq checks produced expected basic groundedness,
  timestamp, insufficient-context and injection responses. These are a small
  smoke sample, not a human-reviewed quality certification.
- Monitoring dashboard `vidora-production` created from existing platform metrics.
- Error policy `3650152950426952242` created with three user-specified email
  channels. Error notification delivery still needs recipient confirmation.
  The policy matches `analysis_failed`, server errors and ERROR-severity logs.

## Outstanding gates / owner input

1. Refresh the failing authorized YouTube session securely, then repeat download
   and warm/cold tests. Never paste cookies or API keys into chat or source files.
2. Supply approved 10-minute and 35-minute test videos; exercise long-video and
   concurrent load behavior and agree a latency target before release sign-off.
3. Confirm alert receipt and monthly budget amount/currency. Budget policy not
   created without an amount; budget notifications are not spending caps.
4. Firestore completion push is not configured. Current source still uses one
   analysis HTTP request and explicit manual recovery, with no recurring browser
   function polling. Durable queued completion, browser authorization and listener
   security must be implemented/tested together before switching this flow.
5. Frontend was built locally, not published to Vercel in this release.
6. Edge identity/abuse controls, Python supply-chain review, backup restore drill,
   independent quality evaluation and organizational risk/management approvals
   remain open. No NIST/ISO certification or guaranteed latency claim is made.

## Reproducible tools

From `functions`: `scripts/release_database.py` (read-only by default),
`scripts/release_database.py --verify-concurrency`,
`scripts/smoke_production.py` (health/access only),
`scripts/smoke_production.py --analyze` (one paid analysis and replay),
`scripts/evaluate_ai.py` (five live synthetic API calls).

Monitoring configuration follows the
[Cloud Monitoring dashboard API](https://docs.cloud.google.com/monitoring/dashboards/api-dashboard)
and [alert policy API](https://docs.cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.alertPolicies).
